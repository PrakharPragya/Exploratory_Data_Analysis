# The objective of this project is to compare the differnet aspects which affect a student's performance through a simple EDA.
# The libraries used for the project are pandas, numpy, matplotlib and seaborn
